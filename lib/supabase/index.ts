export { supabase, createServerClient, createAdminClient } from './client';
export type {
  Database,
  Section,
  SectionType,
  SectionMetadata,
  SkillMetadata,
  ExperienceMetadata,
  ProjectMetadata,
  CertificationMetadata,
  AchievementMetadata,
  Setting,
  User,
  AuditLog,
  AnalyticsEvent,
} from './types';
